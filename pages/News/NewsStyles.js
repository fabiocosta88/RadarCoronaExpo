import {StyleSheet, Dimensions} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
      },
      text: {
          fontSize: 15,
          textAlign: 'center',
          fontWeight: 'bold'
      }
});
